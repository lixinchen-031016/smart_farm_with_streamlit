# 智能农业数据可视化系统 - 用户与管理员手册

## 目录

1. [用户操作手册](#1-用户操作手册)
    - [系统概述](#11-系统概述)
    - [入门指南](#12-入门指南)
    - [核心功能使用](#13-核心功能使用)
    - [高级功能使用](#14-高级功能使用)
    - [智能分析功能](#15-智能分析功能)
    - [系统管理功能](#16-系统管理功能)
    - [常见问题解答](#17-常见问题解答)
2. [管理员维护手册](#2-管理员维护手册)
    - [系统部署](#21-系统部署)
    - [配置管理](#22-配置管理)
    - [用户管理](#23-用户管理)
    - [系统监控](#24-系统监控)
    - [数据备份与恢复](#25-数据备份与恢复)
    - [日志管理](#26-日志管理)
    - [数据库同步](#27-数据库同步)
    - [故障排除](#28-故障排除)

---

## 1. 用户操作手册

### 1.1 系统概述

智能农业数据可视化系统是一个基于 Web 的应用程序，专为现代农业管理设计。系统采用 Streamlit 框架开发，结合 MySQL 数据库和多种机器学习模型，提供全面的农场数据管理和分析能力。

**技术栈**：

- **前端框架**：Streamlit（Python Web 应用框架）
- **后端服务**：Python + SQLAlchemy ORM
- **数据库**：MySQL 8.0+
- **AI 模型**：Ollama 框架（支持 qwen3.5:4b 等）
- **可视化库**：Plotly
- **机器学习**：scikit-learn、Prophet

**系统主要功能模块**：

1. **核心功能**
   - 综合监控仪表板：实时展示农场环境数据和系统状态
   - 实时数据预览：显示最新的传感器数据（温度、湿度、土壤水分、光照等）
   - 数据概览：历史数据查询、上传和导出

2. **数据处理**
   - 数据清洗：删除重复值、处理缺失值、删除列等
   - 数据可视化：散点图、线图、柱状图、热力图等多种图表

3. **数据分析**
   - 数据分析：描述性统计、相关性分析
   - 高级分析：数据分组、聚合分析
   - AI 洞察分析：利用 AI 大模型进行智能解读

4. **预测分析**
   - 本地数据预测：支持单变量和多变量预测
   - 预测模型：SARIMA、Prophet、LSTM、Transformer、随机森林

5. **智能分析**
   - AI 数据洞察：基于 AI 大模型的深度数据分析
   - 自动化决策：生成环境调控建议（灌溉、温度、湿度、光照）
   - 历史报告查看：管理 AI 分析和预测报告

6. **系统管理**
   - 用户管理：用户注册、登录、权限管理
   - 系统监控：CPU、内存、磁盘使用情况监控
   - 日志查看：操作日志查询和分析
   - 数据备份与恢复：定时备份和数据恢复
   - 数据库同步：本地与云端数据库同步
   - 模块配置管理：启用/禁用功能模块

### 1.2 入门指南

#### 1.2.1 系统访问

**系统要求**：

- **浏览器**：Chrome 90+、Firefox 88+、Edge 90+、Safari 14+
- **屏幕分辨率**：推荐 1920x1080 或更高
- **网络连接**：稳定的互联网连接（如使用云端 AI 模型）
- **内存**：至少 4GB 可用内存

**访问步骤**：

1. 打开浏览器，输入系统地址（由管理员提供）
   - 本地部署：`http://localhost:8501`（默认端口）
   - 远程部署：`http://服务器IP:8501`
2. 进入登录页面

#### 1.2.2 用户注册

**注册流程**：

1. 在登录页面点击"注册"按钮
2. 填写注册信息：
   - **用户名**：3-20 个字符，支持字母、数字、下划线
   - **密码**：至少 8 位，建议包含大小写字母、数字和特殊字符
   - **确认密码**：再次输入密码确保一致
   - **角色选择**：
     - 普通用户：基本使用权限
     - 管理员：需要审批，通过后获得完整权限
   - **验证码**：输入图片中的验证码（区分大小写）
3. 点击"注册"按钮完成注册
4. 注册成功后自动跳转到登录页面

> **注意**：如果注册时选择管理员身份，需要等待现有管理员审批通过后才能获得管理员权限。

**密码强度要求**：

- **弱**（红色标识）：长度不足 8 位或字符类型单一
- **中**（黄色标识）：长度 8-11 位或包含两种字符类型
- **强**（绿色标识）：长度≥12 位且包含三种以上字符类型

#### 1.2.3 用户登录

**登录步骤**：

1. 在登录页面输入用户名和密码
2. 输入验证码（点击图片可刷新）
3. 点击"登录"按钮
4. 登录成功后自动跳转到主页（综合监控仪表板）

**安全提示**：

- 连续输错密码 10 次，账户将被锁定 30 秒
- 不要与他人共享账户信息
- 定期更换密码提高安全性

#### 1.2.4 忘记密码

**找回密码流程**：

1. 联系系统管理员
2. 管理员在"用户管理"中重置密码

### 1.3 核心功能使用

#### 1.3.1 实时数据预览

**功能说明**：查看最新的农场环境传感器数据

**操作步骤**：

1. 登录系统后，默认进入"综合监控仪表板"页面
2. 页面显示以下环境指标的当前值：
   - **空气温度**（°C）：实时监测大棚内温度
   - **空气湿度**（%）：实时监测空气相对湿度
   - **土壤湿度**（%）：监测土壤水分含量
   - **土壤养分**：监测土壤营养元素含量
   - **光照强度**（lux）：监测光照条件
3. 数据每分钟自动刷新一次
4. 异常数据会以红色高亮显示

**数据解读**：

- **预警范围**：超出阈值时显示红色并标注具体数值
- **趋势指示器**：箭头显示数据变化趋势（上升↑/下降↓）

**注意事项**：

- 数据来源：直接从数据库中的传感器数据表读取
- 更新频率：默认手动刷新
- 历史对比：部分仪表板支持与预测同期数据对比

#### 1.3.2 数据概览

**功能说明**：查看、管理和导出历史数据

**操作步骤**：

**方式一：从数据库读取**

1. 在左侧菜单选择"数据概览"
2. 选择数据来源："从数据库读取"
3. 选择时间范围：
   - 开始时间：选择起始日期
   - 结束时间：选择结束日期
4. 点击"从数据库读取数据"按钮
5. 系统加载指定时间范围内的所有传感器数据

**方式二：上传本地文件**

1. 在左侧菜单选择"数据概览"
2. 选择数据来源："上传文件"
3. 点击"选择文件"按钮
4. 选择本地数据文件（支持格式）：
   - CSV 文件（.csv）
   - Excel 文件（.xlsx, .xls）
   - JSON 文件（.json）
5. 系统自动解析并加载数据
6. 确保文件包含 `timestamp` 列（时间戳格式）

**数据信息展示**：

- **行数**：数据记录总数
- **列数**：字段数量
- **缺失值数**：统计所有列的缺失值总数
- **数据预览**：显示前几行数据
- **数据类型**：显示每列的数据类型

**数据导出**：

1. 选择导出格式：
   - CSV：适合 Excel 打开和进一步处理
   - Excel：保持格式化的表格文件
   - JSON：适合程序化处理
2. 点击"导出数据"按钮
3. 浏览器自动下载导出的文件

**注意事项**：

- 从数据库读取时，时间范围不宜过大（建议不超过 1 年）
- 上传文件时确保格式正确，否则无法解析
- 导出的数据会自动保存时间戳到文件名

#### 1.3.3 数据清洗

**功能说明**：处理数据质量问题，包括删除重复值、填充缺失值、删除列等

**操作步骤**：

**1. 删除重复行**

1. 在左侧菜单选择"数据清洗"
2. 查看当前数据的重复行统计信息
3. 点击"删除重复行"按钮
4. 系统自动识别并删除完全重复的记录
5. 查看处理后的数据

**2. 处理缺失值**

1. 选择含有缺失值的列名
2. 选择处理方法：
   - **删除法**：直接删除包含缺失值的行（适合缺失比例小的情况）
   - **平均值填充**：用该列的平均值填充缺失值（适合数值型数据）
   - **中位数填充**：用该列的中位数填充（适合有异常值的数据）
   - **众数填充**：用该列的众数填充（适合分类数据）
3. 点击相应的处理按钮执行操作
4. 查看处理后缺失值的变化

**3. 删除不需要的列**

1. 在多选框中选择要删除的列（可多选）
2. 点击"删除选中的列"按钮
3. 确认删除操作
4. 查看删除后的数据结构

**数据质量报告**：

- 显示每列的缺失值数量和百分比
- 显示重复行数量
- 提供数据质量评分

**注意事项**：

- 删除操作不可恢复，建议先备份原始数据
- 选择合适的缺失值处理方法，避免引入偏差
- 删除列前确认该列不包含重要信息

#### 1.3.4 数据分析

**功能说明**：对数据进行统计分析，包括描述性统计和相关性分析

**操作步骤**：

**1. 描述性统计**

1. 在左侧菜单选择"数据分析"
2. 系统自动计算并显示以下统计指标：
   - **计数**（count）：非空值数量
   - **平均值**（mean）：数据的平均水平
   - **标准差**（std）：数据的离散程度
   - **最小值**（min）：数据的最小值
   - **下四分位数**（25%）：第 25 百分位数
   - **中位数**（50%）：第 50 百分位数
   - **上四分位数**（75%）：第 75 百分位数
   - **最大值**（max）：数据的最大值

**2. 相关性分析**

1. 系统自动计算所有数值列之间的相关系数
2. 以热力图形式展示相关性矩阵：
   - 颜色深浅表示相关性强弱
   - 蓝色表示正相关，红色表示负相关
   - 数值范围：-1（完全负相关）到 +1（完全正相关）
3. 解读相关性：
   - **强正相关**（>0.7）：两个变量同向变化
   - **强负相关**（<-0.7）：两个变量反向变化
   - **弱相关**（-0.3~0.3）：关系不明显

**应用场景**：

- 分析温度与湿度的关系
- 研究光照对土壤湿度的影响
- 探索不同环境因子之间的关联性

**注意事项**：

- 相关性不等于因果性
- 异常值可能影响相关性计算结果
- 建议结合专业领域知识解读结果

#### 1.3.5 数据可视化

**功能说明**：以多种图表形式直观展示数据分布和趋势

**操作步骤**：

**1. 选择时间范围**（可选）

1. 在左侧菜单选择"可视化"
2. 使用日期选择器设置起始和结束时间
3. 系统自动筛选该时间范围内的数据

**2. 选择图表类型**

系统提供以下 7 种图表类型：

- **散点图**（Scatter Plot）
  - 用途：展示两个变量之间的关系
  - 适用场景：分析温度与湿度的相关性、光照与土壤湿度的关系
  - 配置：选择 X 轴和 Y 轴列

- **线图**（Line Chart）
  - 用途：展示数据随时间的变化趋势
  - 适用场景：温度日变化、湿度周变化、土壤水分月变化
  - 配置：选择 Y 轴列（X 轴默认为时间）

- **柱状图**（Bar Chart）
  - 用途：比较不同类别的数据
  - 适用场景：对比不同月份的平均温度、各季度的降水量
  - 配置：选择分类列和数值列

- **箱线图**（Box Plot）
  - 用途：展示数据分布和异常值
  - 适用场景：分析温度分布范围、识别异常湿度值
  - 配置：选择要分析的数值列

- **直方图**（Histogram）
  - 用途：展示数据的频率分布
  - 适用场景：温度区间分布、湿度等级分布
  - 配置：选择要分析的列和分组数量

- **饼图**（Pie Chart）
  - 用途：展示各部分占总体的比例
  - 适用场景：不同温度等级的占比、土壤养分等级分布
  - 配置：选择分类列和数值列

- **热力图**（Heatmap）
  - 用途：以颜色深浅展示数据密度或强度
  - 适用场景：温度时空分布、湿度变化热力图
  - 配置：选择 X 轴、Y 轴和颜色映射列

**3. 生成图表**

1. 根据选择的图表类型配置相应参数
2. 点击"生成图表"按钮
3. 系统自动生成交互式图表

**图表交互操作**：

- **缩放**：在图表上拖拽矩形区域进行局部放大
- **平移**：按住 Shift 键并拖拽查看不同区域
- **悬停提示**：鼠标悬停在数据点上显示详细值
- **图例控制**：点击图例项显示/隐藏对应数据系列
- **下载图表**：点击右上角相机图标导出为 PNG 图片

**注意事项**：

- 大数据量时建议选择聚合视图，避免渲染缓慢
- 合理选择图表类型以最佳展示数据特征
- 可调整图表尺寸获得更好的视觉效果

### 1.4 高级功能使用

#### 1.4.1 高级分析

**功能说明**：进行数据分组和聚合分析，支持多维度数据分析

**操作步骤**：

**智能分组分析**

1. 在左侧菜单选择"高级分析"
2. 切换到"📊 智能分组分析"标签页
3. 选择分组列：
   - 例如：按"月份"分组分析月度趋势
   - 例如：按"温度等级"分组分析不同温区的差异
4. 选择聚合列：
   - 选择要统计的数值列（如湿度、土壤水分等）
5. 选择聚合函数：
   - **平均值**：计算每组的平均水平
   - **总和**：计算每组的累计值
   - **最大值**：找出每组的峰值
   - **最小值**：找出每组的最低值
6. 点击"开始分析"按钮

**分析结果解读**：

系统提供通俗易懂的分析结果解释：

- **平均值分析**：
  - 示例："计算了每组月份的空气湿度平均值"
  - 应用："平均湿度可以帮助了解不同季节的湿度状况"

- **最大值分析**：
  - 示例："找出了每组温度等级的最高温度"
  - 应用："最大值可以帮助识别极端情况或最佳表现"

- **智能洞察**：
  - 自动识别最高和最低的分组
  - 示例："💡 智能洞察：湿度最高的分组是 7 月，最低的是 1 月"

- **预警建议**：
  - 基于阈值自动给出建议
  - 示例："⚠️ 最高平均温度达到 35.2°C，可能需要加强通风降温"
  - 示例："⚠️ 最低平均湿度仅为 32.5%，可能需要增加加湿措施"

**详细图表分析**

1. 切换到"📈 详细图表"标签页
2. 选择分组列（图表版）
3. 选择聚合列（图表版）
4. 选择聚合函数（图表版）
5. 点击"生成图表"按钮
6. 系统自动生成柱状图展示分组对比

**应用场景**：

- 按月分析温度、湿度变化趋势
- 按季度对比作物生长环境指标
- 按不同种植区域对比环境条件
- 分析不同时间段的光照强度差异

**注意事项**：

- 分组列应选择有意义的分类变量
- 聚合列必须是数值型数据
- 结合专业知识解读分析结果

#### 1.4.2 本地数据预测

**功能说明**：基于历史数据预测未来趋势，支持单变量和多变量两种预测模式

**操作步骤**：

**模式一：单变量时间序列预测**

1. 在左侧菜单选择"本地数据预测"
2. 切换到"🚀 执行预测"标签页
3. 选择要预测的数据类型：
   - 空气温度（°C）
   - 空气湿度（%）
   - 土壤湿度（%）
   - 土壤养分
   - 光照强度（lux）
4. 选择预测模型：
   - **Prophet+SARIMA(推荐)**：结合两种算法的优势，适合有季节性的数据
   - **纯 Prophet**：适合受节假日影响的数据
5. 设置预测天数：1-30 天
6. 点击"开始预测"按钮
7. 查看预测结果：
   - 预测趋势图表
   - 模型评估指标（RMSE、R²等）
   - 置信区间范围

**模式二：多变量耦合预测**

1. 选择预测模式："多变量耦合预测"
2. 系统同时分析多个变量的相互作用：
   - 温度、湿度、光照三个维度
   - 考虑变量间的耦合关系
3. 设置预测天数
4. 点击"开始预测"按钮
5. 查看分析结果：
   - 多变量趋势对比图
   - 特征重要性分析
   - 随机森林模型解释

**预测结果展示**：

- **趋势图表**：历史数据 + 预测数据的完整趋势
- **评估指标**：
  - RMSE（均方根误差）：越小越好
  - R²（决定系数）：越接近 1 越好
- **特征重要性**：显示各因素对预测结果的影响程度
- **置信区间**：预测值的可能波动范围

**预测历史**：

1. 切换到"📚 预测历史"标签页
2. 查看历史预测记录：
   - 预测 ID、类型、使用的模型
   - 预测天数、RMSE、R²等指标
   - 创建时间
3. 所有预测结果自动保存到 `predictions_exports` 目录

**应用场景**：

- 预测未来一周的温度变化，提前安排农事活动
- 预测湿度趋势，预防高湿病害
- 预测土壤水分，制定灌溉计划
- 多变量联合预测，综合分析环境变化

**注意事项**：

- 预测准确性依赖于历史数据质量和长度
- 建议至少提供 3 个月以上的历史数据
- 不同模型适用于不同类型的数据
- 预测结果仅供参考，需结合实际情况判断


#### 1.4.5 数据库同步

**功能说明**：同步本地和云端数据库之间的数据

**操作步骤**：

1. 在左侧菜单选择"数据库同步"
2. 选择同步方向（本地到云端或云端到本地）
3. 点击"开始同步"
4. 等待同步完成
5. 查看同步结果报告

#### 1.4.6 调试信息

**功能说明**：查看系统调试信息（仅在调试模式下可用）

**操作步骤**：

1. 在左侧菜单选择"调试信息"
2. 查看系统环境信息、资源监控、数据库状态、性能分析等详细信息

### 1.5 智能分析功能

#### 1.5.1 AI 洞察分析

**功能说明**：利用 AI 大模型对农业数据进行智能分析和解读

**操作步骤**：

1. 在左侧菜单选择"AI 洞察分析"
2. 检查 AI 模型可用性：
   - 如模型不可用，可输入模型名称或点击"拉取 AI 模型"
   - 支持切换不同的 AI 模型（如 qwen3.5:4b）
3. 在"数据概览"页面上传或加载数据
4. （可选）在文本框中输入数据背景描述
5. 点击"执行 AI 数据洞察分析"按钮
6. 查看 AI 生成的智能分析结果和建议
7. 分析报告会自动保存到 `ai_insights_exports` 目录

**注意事项**：

- 需要安装 Ollama 并配置相应的 AI 模型
- 支持流式输出分析结果和思考过程
- 自动保存 Markdown 格式的分析报告

#### 1.5.2 历史报告查看

**功能说明**：查看和管理 AI 洞察与数据预测的历史报告

**操作步骤**：

1. 在左侧菜单选择"历史报告查看"
2. 查看报告列表，包括：
   - AI 洞察报告（来自 `ai_insights_exports` 目录）
   - 数据预测报告（来自 `predictions_exports` 目录）
3. 使用搜索功能查找特定报告
4. 点击报告查看详细内容预览
5. 可下载或删除不需要的报告

**报告管理**：

- 支持按时间排序（最新报告优先显示）
- 支持关键词搜索（在文件名、标题和内容中搜索）
- 支持分类查看（全部/AI 洞察/数据预测）

#### 1.5.3 自动化决策

**功能说明**：基于传感器数据和历史趋势生成环境调控建议

**操作步骤**：

1. 在左侧菜单选择"自动化决策"
2. 点击"评估当前环境条件"按钮
3. 系统分析以下内容：
   - 土壤湿度趋势及灌溉建议
   - 温度趋势及保温/降温建议
   - 空气湿度趋势及除湿/增湿建议
   - 光照强度及补光建议
4. 查看生成的决策建议，包括：
   - 决策类型（灌溉、温度、湿度、光照）
   - 优先级（高/中/低）
   - 详细原因说明
   - 当前值和阈值对比

**决策规则**：

- 土壤湿度：低于 30% 建议灌溉，高于 60% 无需灌溉
- 温度：低于 15℃建议保温，高于 30℃建议降温
- 湿度：低于 40% 建议增湿，高于 70% 建议除湿
- 光照：低于 1000lux 建议补光

### 1.6 系统管理功能

#### 1.6.1 数据库同步

**功能说明**：同步本地和云端数据库之间的数据

**操作步骤**：

1. 在左侧菜单选择"数据库同步"
2. 选择同步方向：
   - 本地到云端：将本地数据上传到云端
   - 云端到本地：从云端下载数据到本地
3. 点击"开始同步"按钮
4. 等待同步完成
5. 查看同步结果报告

**注意事项**：

- 需要预先配置云端数据库连接信息
- 同步过程会处理数据冲突
- 建议在同步前先备份重要数据

#### 1.6.2 调试信息

**功能说明**：查看系统调试信息（仅在调试模式下可用）

**操作步骤**：

1. 在左侧菜单选择"调试信息"
2. 查看以下详细信息：
   - 系统环境信息（Python 版本、操作系统等）
   - 资源监控（CPU、内存、磁盘使用情况）
   - 数据库连接状态
   - 性能分析指标
   - 已加载模块列表
   - 配置信息

**适用场景**：

- 系统性能问题排查
- 模块加载状态检查
- 数据库连接问题诊断

### 1.7 常见问题解答

#### 1.7.1 登录相关问题

**Q: 忘记密码怎么办？**
A: 联系管理员重置密码。

**Q: 登录时提示"用户名或密码错误"怎么办？**
A: 检查用户名和密码是否正确，注意大小写。如仍无法登录，请联系管理员。

**Q: 验证码错误怎么办？**
A: 验证码区分大小写，请仔细核对输入。如果看不清验证码，可以点击刷新按钮重新获取。

#### 1.7.2 数据相关问题

**Q: 上传数据文件时提示"不支持的文件类型"怎么办？**
A: 确保文件格式为CSV、Excel或JSON。检查文件扩展名是否正确。

**Q: 数据可视化时图表显示不完整怎么办？**
A: 尝试缩小时间范围或选择其他图表类型。

**Q: 预测结果不准确怎么办？**
A: 预测准确性受数据质量和模型选择影响。尝试使用不同模型或确保数据质量。

#### 1.7.3 系统使用问题

**Q: 页面加载很慢怎么办？**
A: 可能是网络问题或数据量过大。尝试缩小数据范围或联系管理员检查系统性能。

**Q: 图表无法交互怎么办？**
A: 确保使用现代浏览器（Chrome、Firefox、Edge等）。如问题仍存在，请联系技术支持。

**Q: 为什么有些功能看不到？**
A: 系统支持模块化管理，部分功能可能被管理员禁用。如需使用，请联系管理员。

#### 1.7.4 AI 功能相关问题

**Q: AI 模型不可用怎么办？**
A: 
1. 确保已安装 Ollama：访问 https://ollama.ai 下载安装
2. 拉取所需模型：`ollama pull qwen3.5:4b`
3. 在 AI 洞察页面点击"拉取 AI 模型"按钮
4. 或手动输入其他已安装的模型名称

**Q: AI 分析结果生成很慢怎么办？**
A: 
1. 检查网络连接（如果使用在线模型）
2. 选择更小的模型（参数量越小速度越快）
3. 减少数据量或简化数据描述
4. 本地部署模型需要较强的 GPU 支持

**Q: 如何查看历史 AI 分析报告？**
A: 
1. 在左侧菜单选择"历史报告查看"
2. 切换到"AI 洞察"分类
3. 所有自动保存的报告都会显示在列表中
4. 报告文件保存在 `ai_insights_exports` 目录

#### 1.7.5 预测功能相关问题

**Q: 单变量预测和多变量预测有什么区别？**
A: 
- **单变量预测**：只分析一个变量（如温度）的历史数据进行预测
- **多变量预测**：同时分析多个变量（温度、湿度、光照）的相互作用，更准确但计算更复杂

**Q: 如何选择合适的预测模型？**
A: 
- **Prophet+SARIMA(推荐)**：适合有明显季节性和趋势的数据
- **纯 Prophet**：适合有节假日效应的数据


**Q: 预测历史记录在哪里查看？**
A: 
1. 在"本地数据预测"页面切换到"预测历史"标签
2. 查看所有历史预测记录，包括：
   - 预测 ID、类型、模型
   - 预测天数、RMSE、R²等指标
   - 创建时间
3. 预测结果自动保存在 `predictions_exports` 目录

---

## 2. 管理员维护手册

### 2.1 系统部署

#### 2.1.1 环境要求

**硬件配置**：

- **CPU**：4 核以上（推荐 8 核+）
- **内存**：8GB+（推荐 16GB）
- **存储**：40GB+ SSD（推荐 100GB 以上）
- **网络**：稳定的互联网连接

**软件环境**：

- **操作系统**：Linux（推荐 Ubuntu 20.04+）、Windows 10+、macOS 11+
- **Python 版本**：3.11+
- **数据库**：MySQL 8.0+
- **AI 服务**：Ollama（用于本地 AI 模型运行）

**依赖包**：

主要 Python 包包括：
- streamlit：Web 应用框架
- sqlalchemy：数据库 ORM
- pandas：数据处理
- plotly：数据可视化
- scikit-learn：机器学习
- tensorflow/pytorch：深度学习
- prophet：时间序列预测
- bcrypt：密码加密
- pyjwt：JWT 认证

#### 2.1.2 Docker 部署（推荐）

**优势**：

- 一键部署，无需手动配置环境
- 容器化隔离，避免依赖冲突
- 易于扩展和维护
- 包含所有必要的服务和依赖

**部署步骤**：

1. **安装 Docker 和 Docker Compose**
   ```bash
   # Ubuntu/Debian
   curl -fsSL https://get.docker.com | sh
   apt-get install docker-compose
   
   # macOS (使用 Homebrew)
   brew install --cask docker
   ```

2. **克隆项目代码**
   ```bash
   git clone <项目地址>
   cd smart_farm_with_streamlit
   ```

3. **配置环境变量**
   ```bash
   cp .env.example .env
   vim .env  # 编辑配置
   ```
   
   必要配置项：
   - `DATABASE_URL`：MySQL 连接字符串
   - `SECRET_KEY`：应用安全密钥
   - `CLOUD_DATABASE_URL`：云端数据库连接（可选）

4. **启动服务**
   ```bash
   docker-compose up -d
   ```

5. **查看日志**
   ```bash
   docker-compose logs -f
   ```

6. **访问应用**
   - 浏览器打开：`http://localhost:8501`
   - 默认端口：8501（可在 docker-compose.yml 中修改）

**停止服务**：
```bash
docker-compose down
```

**重启服务**：
```bash
docker-compose restart
```

#### 2.1.3 本地部署

**适用场景**：

- 开发和测试环境
- 学习研究用途
- 需要自定义配置的场景

**部署步骤**：

1. **安装 Python 3.11+**
   ```bash
   # Ubuntu/Debian
   sudo apt-get update
   sudo apt-get install python3.11 python3.11-venv python3.11-dev
   
   # macOS (使用 Homebrew)
   brew install python@3.11
   ```

2. **创建虚拟环境**（推荐）
   ```bash
   python3.11 -m venv venv
   source venv/bin/activate  # Linux/macOS
   # 或
   venv\Scripts\activate  # Windows
   ```

3. **安装依赖包**
   ```bash
   pip install -r requirements.txt
   ```

4. **配置数据库**
   - 安装 MySQL 8.0+
   - 创建数据库和用户：
     ```sql
     CREATE DATABASE intelligent_farm;
     CREATE USER 'farm_user'@'localhost' IDENTIFIED BY 'your_password';
     GRANT ALL PRIVILEGES ON intelligent_farm.* TO 'farm_user'@'localhost';
     FLUSH PRIVILEGES;
     ```
   - 导入初始数据：
     ```bash
     mysql -u farm_user -p intelligent_farm < intelligent_farm.sql
     ```

5. **配置环境变量**
   编辑 `.env` 文件：
   ```env
   DATABASE_URL=mysql://farm_user:your_password@localhost:3306/intelligent_farm
   SECRET_KEY=your_secret_key_here
   ```

6. **初始化数据库**
   ```bash
   # 如果数据库表未创建，运行初始化脚本
   python models.py  # 或使用其他初始化脚本
   ```

7. **启动应用**
   ```bash
   streamlit run app.py --server.port=8501
   ```

8. **访问应用**
   - 浏览器打开：`http://localhost:8501`

**后台运行**（可选）：
```bash
nohup streamlit run app.py --server.port=8501 > app.log 2>&1 &
```

#### 2.1.4 AI 模型配置（可选）

如需使用 AI 洞察分析功能，需要安装 Ollama：

1. **安装 Ollama**
   ```bash
   # Linux/macOS
   curl -fsSL https://ollama.ai/install.sh | sh
   
   # Windows: 下载安装包 https://ollama.ai/download
   ```

2. **启动 Ollama 服务**
   ```bash
   ollama serve
   ```

3. **下载 AI 模型**
   ```bash
   ollama pull qwen3.5:4b
   ```

4. **验证模型**
   ```bash
   ollama list  # 查看已安装的模型
   ollama run qwen3.5:4b "Hello"  # 测试模型
   ```

### 2.2 配置管理

#### 2.2.1 模块配置管理

系统通过`module_config.json`文件管理功能模块：

**配置项说明**：

- `enabled`: 模块是否启用
- `display_name`: 显示名称
- `description`: 功能描述
- `category`: 模块分类
- `icon`: 显示图标
- `admin_only`: 是否仅管理员可用
- `dependencies`: 依赖的其他模块

**操作步骤**：

1. 在左侧菜单选择"模块配置管理"
2. 查看各模块状态（已启用/已禁用）
3. 使用切换开关启用/禁用模块
4. 设置模块权限（所有用户/仅管理员）
5. 点击"保存配置"保存更改

**模块分类**：

- 核心功能
- 数据处理
- 数据分析
- 预测分析
- 系统管理
- 数据管理
- 智能分析
- 帮助

**注意事项**：

- 禁用模块会同时对所有用户隐藏该功能
- 某些模块可能存在依赖关系，禁用被依赖的模块会导致依赖它的模块也无法正常工作
- 管理员可以设置模块仅对管理员可见

#### 2.2.2 环境变量配置

在`.env`文件中配置关键参数：

- DATABASE_URL: 数据库连接字符串
- SECRET_KEY: 应用安全密钥

### 2.3 用户管理

#### 2.3.1 用户列表管理

**操作步骤**：

1. 在左侧菜单选择"用户管理"
2. 查看用户列表，包括用户ID、用户名、角色、管理员申请状态等信息

#### 2.3.2 添加用户

**操作步骤**：

1. 点击"添加用户"按钮
2. 输入用户名和密码
3. 选择用户角色（普通用户/管理员）
4. 点击"添加"完成用户创建

#### 2.3.3 编辑用户

**操作步骤**：

1. 在用户列表中找到目标用户
2. 点击"编辑"按钮
3. 修改用户信息
4. 点击"保存"完成修改

#### 2.3.4 删除用户

**操作步骤**：

1. 在用户列表中找到目标用户
2. 点击"删除"按钮
3. 确认删除操作

#### 2.3.5 重置密码

**操作步骤**：

1. 在用户列表中找到目标用户
2. 点击"重置密码"按钮
3. 输入新密码
4. 点击"重置"完成操作

#### 2.3.6 管理员申请审批

**操作步骤**：

1. 在用户管理页面查看"管理员申请审批"部分
2. 查看待审批的管理员申请列表
3. 点击"批准"或"拒绝"按钮处理申请
4. 系统会自动更新用户角色和申请状态

### 2.4 系统监控

#### 2.4.1 实时监控

**监控指标**：

- CPU使用率
- 内存使用率
- 磁盘使用率

**操作步骤**：

1. 在左侧菜单选择"系统监控"
2. 查看实时资源使用情况
3. 查看详细资源信息

#### 2.4.2 性能分析

**分析内容**：

- 系统性能评估
- 优化建议

**操作步骤**：

1. 在"系统监控"页面切换到"性能分析"选项卡
2. 查看系统性能指标
3. 查看优化建议

### 2.5 数据备份与恢复

#### 2.5.1 数据备份

**操作步骤**：

1. 在左侧菜单选择"数据备份"
2. 选择备份时间范围
3. 点击"执行备份"
4. 下载生成的备份文件

**备份文件说明**：

- 包含加密的SQL数据文件
- 包含解密密钥文件
- 两个文件都需要妥善保管

#### 2.5.2 数据恢复

**操作步骤**：

1. 在左侧菜单选择"数据恢复"
2. 上传加密的SQL备份文件
3. 上传密钥文件
4. 点击"恢复数据"
5. 等待恢复完成

**注意事项**：

- 恢复操作会覆盖现有数据
- 确保备份文件和密钥文件匹配
- 建议在恢复前先进行备份

### 2.6 日志管理

#### 2.6.1 日志查看

**操作步骤**：

1. 在左侧菜单选择"日志查看"
2. 选择时间范围
3. 选择日志级别（ALL/INFO/WARNING/ERROR/DEBUG）
4. 选择用户（可选）
5. 点击"查询"查看日志

**日志分析**：

- ERROR级别日志表示系统错误
- WARNING级别日志表示潜在问题
- INFO级别日志表示正常操作记录

**日志导出**：

1. 查询日志后，点击"下载选定日志"按钮
2. 保存日志文件到本地

#### 2.6.2 日志统计

**统计内容**：

- 日志总数
- 各级别日志数量分布

### 2.7 数据库同步

#### 2.7.1 配置云端数据库

**前提条件**：

- 拥有可访问的云端 MySQL 数据库
- 知道云端数据库的连接信息（主机、端口、用户名、密码、数据库名）

**配置步骤**：

1. 在 `.env` 文件中配置云端数据库连接字符串：
   ```
   CLOUD_DATABASE_URL=mysql://username:password@host:port/database_name
   ```
2. 重启应用使配置生效

#### 2.7.2 数据同步操作

**操作步骤**：

1. 在左侧菜单选择"数据库同步"
2. 选择同步方向：
   - **本地→云端**：将本地数据上传到云端备份
   - **云端→本地**：从云端恢复数据到本地
3. 点击"开始同步"按钮
4. 系统会自动：
   - 检测数据差异
   - 处理数据冲突（以时间戳为准）
   - 执行数据同步
5. 查看同步结果报告

**注意事项**：

- 同步前建议先备份当前数据
- 大量数据同步可能需要较长时间
- 确保网络连接稳定

### 2.8 故障排除

#### 2.8.1 常见问题及解决方案

**问题1: 系统无法启动**
可能原因：

- 数据库连接失败
- 环境变量配置错误
- 依赖包缺失

解决方案：

1. 检查数据库服务是否运行
2. 验证`.env`文件配置
3. 重新安装依赖包

**问题2: 数据库连接错误**
可能原因：

- 数据库服务未启动
- 连接字符串错误
- 用户权限不足

解决方案：

1. 启动数据库服务
2. 检查`DATABASE_URL`配置
3. 验证数据库用户权限

**问题 3: 内存不足导致系统缓慢**
可能原因：

- 数据量过大
- 内存泄漏
- 硬件配置不足

解决方案：

1. 优化数据查询，减少一次性加载数据量
2. 重启应用释放内存
3. 升级硬件配置

**问题 4: AI 模型无法使用**
可能原因：

- Ollama 未安装或未启动
- 模型未下载
- 网络连接问题

解决方案：

1. 安装 Ollama：访问 https://ollama.ai
2. 启动 Ollama 服务：`ollama serve`
3. 下载模型：`ollama pull qwen3.5:4b`
4. 检查网络连通性

**问题 5: 数据库同步失败**
可能原因：

- 云端数据库配置错误
- 网络连接不稳定
- 数据冲突处理失败

解决方案：

1. 检查 `.env` 中的云端数据库连接字符串
2. 测试云端数据库连接：`mysql -h host -u username -p`
3. 查看同步日志了解详细错误信息
4. 尝试分批次同步少量数据

#### 2.8.2 系统维护建议

1. 定期备份数据
2. 监控系统性能
3. 定期更新依赖包
4. 清理过期日志文件
5. 监控磁盘空间使用情况
6. 定期审查用户权限和访问日志
7. 定期检查 AI 模型状态并及时更新
8. 监控数据库同步状态确保数据一致性

#### 2.8.3 系统错误日志分析

**查看错误日志**：

1. 在左侧菜单选择"日志查看"
2. 选择日志级别为"ERROR"
3. 选择合适的时间范围
4. 点击"查询"查看错误日志

**常见错误类型及处理**：

- **数据库连接错误**：检查数据库服务状态和连接配置
- **模型加载错误**：确认 AI 模型已正确安装
- **文件操作错误**：检查文件权限和路径
- **内存溢出错误**：优化数据加载或增加系统内存
