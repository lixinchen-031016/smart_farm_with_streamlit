# 智能农场管理系统的设计与实现

**摘要**

随着物联网、大数据和人工智能技术的快速发展，传统农业向智慧农业转型已成为全球农业发展的必然趋势。本文设计并实现了一套基于 Streamlit 框架的智能农场管理系统，该系统集成了实时环境监测、数据分析、混合模型预测（Prophet+SARIMA）、AI 大语言模型洞察、自动化决策等多种功能于一体。系统采用 B/S 三层架构，使用 Python 作为主要开发语言，MySQL 8.0 作为数据库管理系统，结合 Prophet 1.1.7 和 SARIMA 时间序列预测模型以及 Ollama(qwen3:4b) 大语言模型，实现了对农场环境参数的全方位监控和智能化管理。

本系统的主要功能包括：用户认证与权限管理（JWT+bcrypt）、综合监控仪表板（实时监测 + 异常检测 + 短期预测）、数据概览与导出、数据清洗与预处理、统计分析、数据可视化、高级分析、本地数据预测（单变量 + 多变量 + 混合模型）、AI 洞察分析（流式输出 + 提示工程）、自动化决策（规则引擎 + 趋势分析）、系统监控（psutil）、日志管理、数据备份与恢复、模块配置管理（动态启用/禁用）等模块。系统采用了模块化设计理念，通过 module_config.json 配置文件支持动态启用/禁用功能模块，具有良好的可扩展性和可维护性。

创新性地提出了基于 RMSE 的自适应权重分配算法，将 Prophet 和 SARIMA 模型进行融合，根据两个模型的均方根误差动态调整权重，提升预测精度。设计了结构化的农业领域提示工程模板，使通用 AI 模型能够理解农业数据特征并提供专业建议。引入趋势分析增强决策准确性，计算过去 24 小时数据的线性回归斜率，结合当前值和趋势生成灌溉、通风、补光等农事操作建议。

测试结果表明，该系统运行稳定，界面友好，功能完善，能够有效提升农场管理的智能化水平，降低人工成本，提高农业生产效率，具有较高的实用价值和推广前景。系统完全基于开源技术构建，对硬件要求较低，适合中小规模农场使用。

**关键词**：智能农场；Streamlit；时间序列预测；SARIMA；Prophet；混合模型；Ollama；大语言模型；AI 洞察；模块化设计；异常检测；自动化决策


---

# 第 1 章 绪论

## 1.1 研究背景

农业作为国民经济的基础产业，关系到国家粮食安全和社会稳定。随着全球人口持续增长和耕地面积不断减少，传统农业面临着资源短缺、环境污染、劳动力不足等诸多挑战。根据联合国粮农组织（FAO）统计数据，到 2050 年全球人口将达到 97 亿，粮食需求将增长 70%，而可用耕地面积却在持续减少 [1]。如何在有限的土地资源上提高农业生产效率，成为亟待解决的重大问题。

近年来，物联网（IoT）、大数据、云计算、人工智能等新一代信息技术的快速发展，为农业现代化提供了新的解决方案。智慧农业（Smart Agriculture）概念应运而生，它通过传感器网络实时采集农田环境数据，利用大数据分析技术挖掘数据价值，借助人工智能算法实现精准决策，最终达到提高产量、降低成本、保护环境的目的 [2]。

在政策层面，中国政府高度重视智慧农业发展。2023 年中央一号文件明确提出要"发展智慧农业，建立农业农村大数据体系"。《"十四五"全国农业农村信息化发展规划》也强调要推进信息技术与农业农村深度融合，加快发展智慧农业 [3]。这些政策的出台为智慧农业发展提供了强有力的支持。

然而，当前智慧农业发展仍面临一些问题：一是系统建设成本高，中小农户难以承担；二是数据利用率低，大量监测数据未被有效分析；三是智能化程度不足，多数系统仅停留在数据采集和展示层面；四是用户体验差，操作复杂，农民接受度低 [4]。因此，开发一套成本低廉、功能完善、易于使用、智能化程度高的农场管理系统具有重要的现实意义。

本研究旨在设计并实现一套基于 Streamlit 框架的智能农场管理系统，通过集成先进的数据分析算法和 AI 大模型，为农场管理者提供全方位、智能化的决策支持，推动传统农业向数字化、智能化方向转型升级。

## 1.2 国内外研究现状

### 1.2.1 国外研究现状

发达国家在智慧农业领域起步较早，已经形成了较为完善的技术体系和产业生态。

美国在精准农业技术方面处于世界领先地位。John Deere 公司开发的 FarmSight 系统集成了 GPS 导航、自动驾驶、变量施肥等功能，能够实现厘米级精度的田间作业管理。Trimble 公司的 Ag Software 套件提供了从种植规划到收获分析的全流程数字化管理工具 [5]。这些商业系统功能强大，但价格昂贵，主要面向大型农场。

欧洲国家注重农业可持续发展，在环境监测和生态保护方面具有优势。荷兰瓦赫宁根大学研发的 Greenhouse Climate Control 系统通过多传感器融合和模型预测控制，实现了温室环境的精准调控，节能效果显著 [6]。德国 Bosch 公司的 Smart Farming 解决方案利用 IoT 技术和云平台，为中小型农场提供经济实惠的数字化服务。

日本在植物工厂和设施农业方面技术领先。Spread 公司的"Techno Farm"实现了从播种到收获的全程自动化，人均管理面积是传统农场的 10 倍以上 [7]。松下、东芝等企业开发了基于 LED 光照控制和营养液循环的智能化栽培系统。

在技术研究方面，机器学习算法在农业预测中得到广泛应用。Liu 等人 [8] 提出了基于 LSTM 网络的作物产量预测模型，准确率达到 85% 以上。Zhang 等人 [9] 利用随机森林算法进行病虫害预警，提前 7 天预测准确率达 78%。深度学习技术也在农业图像识别、生长监测等方面取得突破。

总体来看，国外智慧农业呈现以下发展趋势：一是从单点技术应用向全流程数字化转型；二是从数据采集展示向智能分析决策升级；三是从大型农场向中小农户普及；四是注重数据隐私保护和系统互操作性。

### 1.2.2 国内研究现状

我国智慧农业研究起步较晚但发展迅速，在政策支持和技术进步的双重驱动下取得了显著成就。

在科研方面，中国农业大学李道亮教授团队开发了设施农业环境智能监控系统，实现了基于机器视觉的作物生长状态识别和环境参数自适应调控 [10]。中国科学院自动化研究所提出了基于深度学习的温室番茄产量预测方法，预测误差小于 10%[11]。南京农业大学研发了水稻精准栽培决策支持系统，能够根据气象数据和土壤条件给出最优施肥方案 [12]。

在企业应用方面，阿里巴巴推出的"ET 农业大脑"整合了语音识别、图像识别、自然语言处理等 AI 技术，在养猪场实现了疾病预警、精准饲喂等智能化功能 [13]。腾讯的"AI 生态鹅厂"项目利用计算机视觉技术监测家禽生长状况，优化养殖管理流程。京东数科的智慧农业平台提供了从生产到销售的全产业链数字化解决方案 [14]。

在产业化推广方面，山东省寿光市建立了蔬菜智慧生产管理平台，覆盖全市 80% 以上的蔬菜大棚，平均增产 15%，节本增效显著 [15]。浙江省湖州市打造了"数字渔业"示范工程，通过水质在线监测和智能增氧，降低了鱼类死亡率，提高了养殖效益 [16]。

然而，我国智慧农业发展仍存在一些问题：一是核心技术创新能力不足，高端传感器和智能装备依赖进口；二是数据标准不统一，不同系统之间难以互联互通；三是重硬件轻软件，数据分析和服务能力薄弱；四是缺乏既懂农业又懂信息技术的复合型人才 [17]。

针对上述问题，本研究设计的智能农场管理系统具有以下特点：一是采用开源技术栈，降低系统成本；二是强化数据分析功能，提升数据价值挖掘能力；三是引入 AI 大模型，增强智能决策水平；四是注重用户体验，简化操作流程。通过这些创新，期望为我国智慧农业发展提供有益探索。

## 1.3 主要研究意义和内容

### 1.3.1 研究的意义

本研究设计并实现的智能农场管理系统具有重要的理论意义和实践价值。

**理论意义**：

第一，丰富了智慧农业系统的理论体系。现有研究多集中于单一技术应用或特定场景解决方案，本研究从系统论角度出发，构建了集数据采集、分析、预测、决策于一体的完整框架，为智慧农业系统设计提供了新思路 [18]。系统采用模块化设计理念，通过 module_config.json 配置文件实现功能模块的动态启用/禁用，支持依赖关系管理和优先级排序，这种灵活的架构设计为农业软件系统工程化提供了新的方法论 [26]。

第二，拓展了时间序列预测算法在农业领域的应用。针对农业环境数据的特点，创新性地将 Prophet 和 SARIMA 模型进行融合，提出了基于 RMSE 的自适应权重分配算法。如公式 (1-1) 所示，根据两个模型的均方根误差动态调整权重，使得预测精度更高的模型获得更大的权重系数，从而提升整体预测性能。该方法为农业数据预测提供了新方法 [19]。

$$w_{prophet} = \frac{1/RMSE_{prophet}}{1/RMSE_{prophet} + 1/RMSE_{SARIMA}} \tag{1-1}$$

第三，探索了大语言模型在农业数据分析中的应用模式。通过构建农业领域知识提示工程（Prompt Engineering），设计了包含数据概览、关键指标解读、趋势分析、关联关系、异常检测、农业建议、后续行动等 7 个维度的结构化提示模板，使通用 AI 模型（qwen3:4b）能够理解农业数据特征并提供专业建议，推动了 AI 技术与农业的深度融合 [20]。系统采用流式输出技术，显著降低了用户等待焦虑，提升了交互体验 [48]。

第四，提出了基于规则引擎的自动化决策模型。将农艺专家知识转化为可计算的决策规则，结合实时监测数据和 24 小时变化趋势（线性回归斜率），生成灌溉、通风、补光、施肥等农事操作建议。决策优先级分为高、中、低三个等级，并附带详细的原因分析和时间戳信息，实现了从数据监测到智能决策的闭环 [25]。

**实践价值**：

第一，降低智慧农业应用门槛。系统完全基于开源技术构建，包括 Streamlit、Prophet、SARIMA、Ollama 等，无需昂贵的商业软件授权费用。模块化设计允许用户根据实际需求选择功能，通过 module_config.json 配置文件即可启用或禁用某个模块，避免资源浪费。系统对服务器硬件要求较低，普通云服务器（2 核 4G）即可满足运行需求，中小农户也能负担得起 [53]。

第二，提升农场管理决策水平。通过综合监控仪表板实时监测环境参数（温度、湿度、土壤湿度、光照强度），利用 Plotly 交互式图表分析变化趋势，基于 Prophet+SARIMA 混合模型预测未来发展（支持 1-30 天预测），通过 Ollama 大语言模型生成智能决策建议，帮助农场主及时发现问题、科学制定方案，减少经验主义带来的风险 [21]。系统还支持异常检测（IQR 方法），当检测到数据异常时自动告警并标记在趋势图上 [36]。

第三，促进农业可持续发展。精准的环境控制和资源投入可以减少化肥农药使用，降低面源污染，保护生态环境。数据驱动的精细化管理也有助于节约水资源和能源消耗。例如，当土壤湿度低于 30% 且呈下降趋势时，系统会生成"立即灌溉"的高优先级建议；当温度高于 30℃时，系统会建议通风降温。这些智能决策有助于实现节能减排目标 [22]。

第四，培养新型职业农民。系统的使用过程也是农民学习信息技术、掌握数字技能的过程。通过直观的图形界面、清晰的引导提示、详细的在线帮助，农民可以快速上手使用系统。系统提供的 AI 洞察分析功能还能解释数据背后的农业科学原理，帮助用户提升专业认知水平，有助于培育懂技术、善经营的新型农业经营主体，为乡村振兴提供人才支撑 [23]。

### 1.3.2 研究的主要内容

本研究围绕智能农场管理系统的设计与实现展开，主要内容包括以下几个方面：

**第一，系统需求分析与总体设计**。通过调研农场管理实际需求和现有系统功能缺陷，明确系统建设目标和功能定位。采用软件工程方法进行需求分析，绘制用例图和流程图。基于 Streamlit 框架设计 B/S 三层架构（表示层、业务逻辑层、数据访问层），确定技术路线和开发方案。系统采用延迟导入（lazy_importer）机制优化加载性能，通过预加载关键模块（preload_modules）提升用户体验 [89]。

**第二，数据库设计与实现**。根据农场环境监测数据类型和特点，设计合理的数据库表结构。建立空气温湿度（intelligent_farm_airtemperaturehumidity）、土壤湿度（intelligent_farm_soilmoisture）、土壤养分（intelligent_farm_soilnutrient）、光照强度（intelligent_farm_lightintensity）等传感器数据表，以及用户信息表（user）、操作日志表（operation_log）、系统配置表（system_config）等管理数据表。所有表使用 InnoDB 存储引擎，支持事务和外键约束。为 timestamp 字段创建索引加速时间范围查询，使用 SQLAlchemy 连接池管理数据库连接，提高并发性能 [24][40]。

**第三，核心功能模块开发与实现**。重点完成以下功能模块：

- **用户认证与权限管理**：实现基于 JWT 的无状态身份验证和基于角色的访问控制（RBAC）。登录时生成包含用户名、角色、过期时间的 JWT 令牌（有效期 14 天）。使用 bcrypt 对用户密码进行加盐哈希加密存储，防止彩虹表攻击。实现图形验证码生成与验证（PIL 库），连续多次登录失败后账户锁定，防止暴力破解。管理员拥有全部权限，普通用户只能访问授权功能 [42][43]。

- **综合监控仪表板**：实时展示空气温度、空气湿度、土壤湿度、土壤养分、光照强度等关键指标，每个指标显示当前值、状态（正常/异常）、适宜范围提示。使用 Plotly 绘制 24 小时变化趋势曲线，支持缩放、平移、悬停查看具体数值。在趋势图上用红色 X 标记异常数据点（基于 IQR 方法检测），叠加显示未来 1-7 天的预测趋势（虚线表示）。支持用户自定义阈值配置，根据不同作物生长阶段（生长期、开花期、结果期）自动推荐适宜的阈值范围 [36]。

- **数据分析模块**：提供描述性统计（均值、标准差、最小值、最大值、四分位数）、相关性分析（Pearson 相关系数矩阵，热力图可视化）、分组聚合分析（按日期、时间段分组，支持多种聚合函数）、数据分布分析（直方图、密度曲线、Q-Q 图）。使用通俗易懂的语言解释统计分析结果，自动识别强相关变量对（相关系数绝对值大于 0.7）[38]。

- **数据预测模块**：集成 Prophet 和 SARIMA 模型，支持单变量和多变量预测。Prophet 模型采用加法季节性模式，适合处理具有强烈季节效应的农业数据。SARIMA 模型通过 ADF 检验平稳性，自动搜索最优参数组合 (p,d,q)(P,D,Q,s)。创新性地提出混合预测模型，根据两个模型的 RMSE 动态分配权重，提升预测精度。多变量预测使用随机森林算法（RandomForestRegressor），添加滞后特征（temp_lag1, humid_lag1）和交互项（temp_humid_interaction），捕捉温度、湿度、光照等环境因子之间的非线性关系 [44][45][46]。

- **AI 洞察模块**：利用 Ollama 大语言模型（qwen3:4b）对数据分析结果进行智能解读。设计结构化的农业领域提示工程模板，包含数据概览、关键指标解读、趋势分析、关联关系、异常检测、农业建议、后续行动等 7 个维度。支持流式输出，逐字显示 AI 回答，减少用户等待焦虑。支持模型切换和一键拉取未安装的模型 [20][48]。

- **自动化决策模块**：基于规则引擎生成农事操作建议。预定义土壤湿度、温度、湿度、光照的告警阈值和决策规则。例如：土壤湿度低于 30% 建议灌溉，高于 60% 无需灌溉；温度低于 15℃建议保温，高于 30℃建议通风。引入趋势分析增强决策准确性，计算过去 24 小时数据的线性回归斜率，若土壤湿度低于阈值且呈下降趋势（slope < 0），则生成"立即灌溉"的高优先级建议。决策建议包含问题描述、原因分析、优先级、推荐措施、时间戳等信息 [25]。

- **系统管理模块**：包括用户管理（添加、删除、修改用户信息，审批管理员申请）、系统监控（CPU 使用率、内存使用率、磁盘使用率、网络流量实时监控，基于 psutil 库）、日志查看（操作日志记录，支持筛选、导出、统计分析）、数据备份恢复（定时自动备份，手动备份，增量备份，恢复验证）、模块配置管理（动态启用/禁用模块，依赖关系检查，配置导入导出）[47]。

**第四，模块化架构设计与实现**。设计灵活的模块配置管理系统，通过 JSON 配置文件（module_config.json）管理所有功能模块。每个模块定义名称（name）、启用状态（enabled）、显示名称（display_name）、分类（category）、管理员专用标识（admin_only）、依赖关系（dependencies）等属性。实现模块管理器（ModuleManager）类，提供模块列表查询、状态切换、依赖检查、分类筛选等功能。禁用的模块在侧边栏菜单中自动隐藏，不影响用户使用其他功能。这种设计使得系统具有良好的可扩展性和可维护性，新增功能模块只需在配置文件中添加相应条目即可 [26][89]。

**第五，系统测试与性能优化**。制定详细的测试计划，开展功能测试（登录、数据查询、预测、AI 分析等）、性能测试（页面加载时间、并发用户数、预测模型训练时间）、安全测试（SQL 注入、XSS 攻击、CSRF 攻击、暴力破解）、兼容性测试（Chrome、Firefox、Safari、Edge浏览器，Windows、Linux、macOS操作系统）。针对测试中发现的问题进行修复和优化。采用延迟导入机制优化模块加载性能，使用@st.cache_data 和@st.cache_resource 装饰器缓存计算结果，对大数据集进行采样展示，优化数据库查询语句（使用索引、减少 JOIN 操作）等措施提高系统响应速度 [27]。

**第六，用户界面设计与体验优化**。遵循人机工程学原则设计直观友好的图形界面。采用卡片式布局展示实时指标，渐变色主题美化视觉效果，Plotly 交互式图表增强数据探索能力。侧边栏使用 streamlit-option-menu 实现美观的导航菜单，支持图标和自定义样式。提供详细的使用说明（USER_ADMIN_MANUAL.md）和在线帮助，降低学习成本。移动端适配确保用户可以通过手机随时查看农场数据和接收告警通知 [28][35][36]。

通过上述研究内容的实施，最终形成一个功能完善、性能优良、易于使用的智能农场管理系统，为农业生产提供全方位的数字化服务。

---

# 第 2 章 相关技术介绍

## 2.1 开发工具

本系统开发过程中使用的主要工具如下：

**PyCharm Professional**：JetBrains 公司出品的 Python 集成开发环境，提供代码编辑、调试、版本控制、数据库工具等全方位功能。其智能代码补全、实时语法检查、重构工具等特性极大提高了开发效率。本项目利用 PyCharm 的虚拟环境管理和依赖包管理功能，确保开发环境的一致性和可移植性 [29]。

**Git**：分布式版本控制系统，用于代码版本管理和协作开发。通过 Git 可以追踪代码变更历史、管理分支、合并代码冲突。本项目使用 Git 进行日常开发管理，并通过.gitignore 文件排除不必要的文件，保持代码仓库整洁 [30]。

**Docker**：容器化技术，用于打包应用及其依赖环境，实现"一次构建，到处运行"。本项目编写了 Dockerfile 和 docker-compose.yml 文件，便于在不同环境中快速部署系统，也方便与其他开发者共享开发环境 [31]。

**Navicat Premium**：数据库管理和开发工具，支持 MySQL、PostgreSQL、SQLite 等多种数据库。提供可视化的表结构设计、数据导入导出、SQL 查询编写等功能。在本项目中用于数据库设计、数据迁移和日常维护 [32]。

**Postman**：API 开发和测试工具，用于模拟 HTTP 请求、测试接口功能、调试 Web 服务。虽然本系统主要使用 Streamlit 框架构建前端，但在某些后台服务开发中仍使用 Postman 进行接口测试 [33]。

## 2.2 前端技术

本系统前端完全基于 Python 技术栈实现，主要使用以下技术：

**Streamlit**（版本 1.45.1）：专为机器学习和数据科学项目设计的开源 Web 应用框架。与传统 Web 框架不同，Streamlit 允许开发者纯 Python 代码快速构建交互式数据应用，无需编写 HTML、CSS、JavaScript 等前端代码。其核心优势包括 [34]：
- 简洁的 API：通过 st.write()、st.dataframe()、st.plotly_chart() 等简单函数即可实现丰富功能
- 自动热重载：代码修改后页面自动刷新，无需手动重启服务器
- 内置组件：提供按钮、输入框、下拉菜单、滑块、卡片、指标板等 40 多种 UI 组件
- 数据可视化：原生支持 Matplotlib、Plotly、Altair 等可视化库
- 会话状态管理：通过 st.session_state 管理用户交互状态，实现跨页面数据共享
- 缓存机制：通过@st.cache_data 和@st.cache_resource装饰器优化性能，避免重复计算
- 主题定制：支持自定义颜色主题、字体大小、布局样式

**Streamlit 扩展组件**：为增强系统功能，引入了多个 Streamlit 第三方扩展库：
- streamlit-extras（0.7.1）：提供额外的精美组件和样式，如 metric_cards 用于美化指标卡片显示，styler 用于表格样式定制 [35]
- streamlit-option-menu（0.4.0）：实现侧边栏菜单导航，支持图标（FontAwesome）、自定义样式、选中状态高亮
- streamlit-camera-input-live（0.2.0）：支持摄像头实时输入，为未来扩展视频监控功能预留接口
- streamlit-toggle-switch（1.0.2）：提供美观的开关切换组件，用于模块启用/禁用控制
- streamlit-vertical-slider（2.5.5）：垂直滑动条组件，用于阈值调节等场景，支持多滑块范围选择 [36]
- streamlit-card（1.0.2）：卡片式布局组件，增强视觉效果
- st-theme（1.2.3）：自定义主题样式，支持深色/浅色模式切换
- streamlit-avatar（0.1.3）：用户头像显示组件

**Plotly**（版本 5.24.1）：交互式可视化库，支持创建超过 40 种图表类型。相比 Matplotlib，Plotly 图表具有更好的交互性，支持缩放、平移、悬停提示、图例点击隐藏等功能。本系统使用 Plotly 绘制温度、湿度等环境参数的时间序列曲线图、热力图、散点图、箱线图、直方图等。Plotly 与 Streamlit 集成良好，通过 st.plotly_chart() 即可直接渲染交互式图表 [37]。

**Pandas**（版本 2.2.3）：强大的数据处理和分析库，提供 DataFrame 和 Series 两种核心数据结构。在本系统中用于数据读取（read_sql_query 从数据库加载）、清洗（处理缺失值、异常值）、转换（数据类型转换、特征工程）、聚合（groupby 分组聚合）、统计（describe 描述性统计、corr 相关系数矩阵）等各种数据操作。Pandas 的高效性能使得处理大规模传感器数据成为可能 [38]。

**NumPy**（版本 1.26.4）：科学计算基础库，提供高性能的多维数组对象（ndarray）和各种数学函数（线性代数、傅里叶变换、随机数生成等）。虽然本系统主要使用 Pandas 进行数据处理，但底层仍依赖 NumPy 的数组运算能力。例如在 SARIMA 模型参数搜索过程中，使用 NumPy 进行网格搜索（grid search）快速评估参数组合 [39]。

**Matplotlib**（版本 3.10.3）：经典的静态可视化库，用于绘制部分静态图表和 Q-Q 图（正态性检验）。虽然交互性不如 Plotly，但在出版级图表质量和精细控制方面仍有优势 [37]。

## 2.3 后端技术

系统后端采用 Python 语言开发，使用了以下核心技术和框架：

**SQLAlchemy**（版本 2.0.41）：Python SQL 工具包和对象关系映射（ORM）框架。它允许开发者使用 Python 类和对象来表示数据库表和记录，通过面向对象的方式操作数据库，而无需直接编写 SQL 语句。SQLAlchemy 的核心优势包括 [40]：
- 声明式模型定义：通过继承 declarative_base() 类定义数据模型，使用 Column 定义字段，指定主键、外键、索引等约束
- 灵活的查询接口：支持链式调用（filter、order_by、limit 等）和条件过滤，可构造复杂查询
- 事务管理：自动处理数据库事务，保证数据一致性，支持显式事务控制（session.begin()）
- 连接池：高效管理数据库连接，通过 create_engine 配置连接池参数（pool_size、max_overflow），提高并发性能
- 支持多种数据库：兼容 MySQL、PostgreSQL、SQLite 等主流数据库，通过 PyMySQL 连接 MySQL

本项目定义了 AirTemperatureHumidity（空气温湿度）、SoilMoisture（土壤湿度）、SoilNutrient（土壤养分）、LightIntensity（光照强度）、User（用户）、OperationLog（操作日志）等数据模型，并通过 SQLAlchemy 进行数据持久化操作。使用 session.add()、session.commit()、session.query() 等方法完成增删改查操作。

**PyMySQL**（版本 1.1.1）：纯 Python 实现的 MySQL 客户端库，用于连接和操作 MySQL 数据库。相比 MySQLdb 等其他 MySQL 驱动，PyMySQL 具有安装简单（pip install 即可）、跨平台（Windows、Linux、macOS通用）、兼容性好（支持 Python 3.8+）等优点。本项目通过 PyMySQL 建立与 MySQL 数据库的连接，连接字符串格式为：mysql+pymysql://username:password@host:port/database[41]。

**bcrypt**（版本 4.3.0）：密码哈希库，用于对用户密码进行加密存储。bcrypt 基于 Blowfish 加密算法，具有以下安全特性 [42]：
- 加盐哈希：为每个密码生成随机盐值（salt），即使两个用户密码相同，加密后的哈希值也不同，有效防止彩虹表攻击
- 可调成本因子：通过调整迭代次数（cost factor，默认为 12）增加暴力破解难度，每增加 1 成本翻倍
- 抗 GPU 攻击：内存密集型算法，降低 GPU 并行破解效率，比传统的 SHA-256 等哈希算法更安全

本系统在用户注册和修改密码时使用 bcrypt 对密码进行加密（bcrypt.hashpw(password, bcrypt.gensalt())），登录时进行验证（bcrypt.checkpw(password, hashed)）。

**PyJWT**（版本 2.10.1）：JSON Web Token 实现库，用于生成和验证 JWT 令牌。JWT 是一种开放标准（RFC 7519），用于在网络应用环境间安全地传输信息。JWT 令牌由三部分组成：头部（Header，包含算法和令牌类型）、载荷（Payload，包含用户信息和过期时间）、签名（Signature，使用密钥对头部和载荷签名）。本系统使用 JWT 实现无状态认证机制，用户登录成功后生成包含用户名、角色、过期时间的令牌（有效期 14 天），后续请求携带该令牌进行身份验证。JWT 的优势包括 [43]：
- 无状态：服务器不需要保存会话信息，减轻服务器负担
- 可扩展：易于跨域共享，适合分布式系统
- 自包含：载荷中包含所有必要信息，减少数据库查询

**Prophet**（版本 1.1.7）：Facebook 开源的时间序列预测库，基于可加模型原理，擅长处理具有强烈季节效应和历史数据的时间序列。Prophet 的主要特点包括 [44]：
- 自动检测趋势变化点和季节性模式：使用加法模型分解趋势项、季节项、节假日项
- 对缺失值和异常值具有鲁棒性：基于稳健的统计方法，不易受异常数据影响
- 支持自定义节假日效应：可添加自定义的节假日列表，建模节假日对时间序列的影响
- 训练速度快，参数调优简单：默认参数即可获得不错的效果，也支持手动调节 changepoint_prior_scale（变化点灵敏度）、seasonality_prior_scale（季节性强度）等超参数

本系统集成 Prophet 进行环境温度、湿度等参数的短期预测（1-30 天），采用加法季节性模式（seasonality_mode='additive'），针对农业数据优化参数设置，并取得良好效果。

**scikit-learn**（版本 1.6.1）：机器学习库，提供了丰富的监督学习和无监督学习算法。本系统使用 scikit-learn 中的 RandomForestRegressor（随机森林回归器）进行多变量耦合预测。随机森林是一种集成学习方法，通过构建多个决策树并取平均值来提高预测精度和稳定性。在多变量预测中，添加滞后特征（lag features，如前一时刻的温度、湿度）和交互项（interaction terms，如温度×湿度），捕捉温度、湿度、光照等环境因子之间的非线性关系。随机森林的优势包括 [45]：
- 能处理高维数据：支持多特征输入，自动评估特征重要性
- 不易过拟合：通过随机采样和特征子集控制模型复杂度
- 可解释性强：提供 feature_importances_属性，解释各变量对预测的贡献

**statsmodels**（版本 0.14.4）：统计建模库，提供各类统计模型和时间序列分析方法。本系统使用其中的 SARIMAX 模型进行季节性时间序列预测。SARIMA（季节性自回归积分滑动平均模型）是在 ARIMA 基础上增加季节性分量，适合处理具有周期性变化的数据，在农业环境监测数据预测中表现优异。SARIMA 模型表示为 ARIMA(p,d,q)(P,D,Q)s，其中 [46]：
- p：自回归（AR）阶数
- d：差分（I）阶数
- q：移动平均（MA）阶数
- P、D、Q：季节性 AR、I、MA 阶数
- s：季节周期长度（如日数据 s=24，周数据 s=7）

本系统通过 ADF 检验（Augmented Dickey-Fuller test）判断时间序列平稳性，若不平稳则进行差分处理。使用网格搜索（grid search）遍历参数组合，根据 AIC（赤池信息量准则）选择最优参数。

**psutil**（版本 7.0.0）：跨平台系统监控库，用于获取 CPU、内存、磁盘、网络等系统资源使用情况。本系统在系统监控模块中使用 psutil 实时展示服务器运行状态，帮助管理员及时发现性能瓶颈。主要功能包括 [47]：
- CPU 监控：获取 CPU 使用率（cpu_percent()）、CPU 核心数、CPU 频率
- 内存监控：获取总内存、已用内存、剩余内存、使用百分比（virtual_memory()）
- 磁盘监控：获取磁盘分区、总空间、已用空间、剩余空间（disk_usage()）
- 网络监控：获取网络收发字节数、速率（net_io_counters()）
- 进程监控：获取进程列表、进程占用的 CPU 和内存资源（process_iter()）

**Ollama**：本地大语言模型运行框架，允许在本地服务器上部署和运行开源 LLM 模型，无需联网即可使用。本系统通过 Ollama 集成 qwen3:4b（通义千问 4B 参数版本）等模型，为 AI 洞察分析提供强大的自然语言处理能力。Ollama 的优势包括 [48]：
- 本地部署：数据不出本地服务器，保护隐私和数据安全
- 模型丰富：支持 Llama、Qwen、Mistral 等多种开源模型
- 简单易用：通过简单的 API 调用即可实现文本生成、对话等功能
- 流式输出：支持逐字输出，减少用户等待焦虑

**APScheduler**（版本 3.11.0）：高级任务调度库，用于定时执行后台任务。本系统使用 APScheduler 实现定时数据备份、定时清理过期日志等周期性任务。支持 cron、interval、date 三种触发器，可精确控制任务执行时间 [5]。

## 2.4 数据库技术

**MySQL 8.0**：Oracle 公司开发的关系型数据库管理系统，是全球最流行的开源数据库之一。MySQL 8.0 相比之前版本有诸多改进 [49]：
- 性能提升：读写吞吐量提高 2-5 倍，特别是高并发场景下表现更佳
- JSON 增强：支持原生 JSON 文档存储和查询，提供虚拟列等功能
- 安全性增强：默认使用 caching_sha2_password 认证插件，角色管理更加精细
- 窗口函数：支持分析函数和窗口函数，便于复杂数据分析
- CTE 支持：公共表表达式使复杂查询更简洁易读

本系统选择 MySQL 8.0 作为数据存储后端，主要基于以下考虑：
1. 成熟稳定：经过多年发展，MySQL 在生产环境中得到广泛验证
2. 易于维护：安装配置简单，社区活跃，文档丰富
3. 成本效益：开源免费，适合中小型项目
4. 生态完善：与各种开发工具和框架集成良好

**数据库设计规范**：
- 所有表使用 InnoDB 存储引擎，支持事务和外键约束
- 主键采用自增整数或时间戳，确保唯一性
- 时间字段统一使用 DATETIME 类型，便于时间范围查询
- 数值字段根据精度要求选择 FLOAT、DOUBLE 或 DECIMAL
- 为频繁查询的字段创建索引，优化查询性能
- 使用外键维护表间关系，保证数据完整性 [50]

**数据库优化措施**：
- 合理设计表结构，避免数据冗余
- 为 timestamp 字段创建索引，加速时间范围查询
- 使用连接池管理数据库连接，减少连接开销
- 定期执行 ANALYZE TABLE 更新统计信息，优化查询计划
- 对大表进行分区或分表，提高查询效率 [51]

---

# 第 3 章 系统分析

## 3.1 可行性分析

**技术可行性**：

本系统采用的技术栈均为成熟稳定的开源技术。Streamlit 框架自 2018 年发布以来发展迅速，已有超过 2000 个 GitHub Star 和活跃的社区支持。SQLAlchemy、Pandas、Plotly 等库在数据科学领域得到广泛应用，文档完善，示例丰富。Prophet 和 SARIMA 等预测算法经过多年理论研究和实践检验，在各类时间序列预测任务中表现可靠。Ollama 框架使得在本地部署大语言模型变得简单可行。开发团队具备 Python 开发、数据库设计、机器学习等方面的技术能力，能够胜任系统开发工作。因此，从技术角度看，本项目完全可行 [52]。

**经济可行性**：

系统开发成本主要包括人力成本、硬件成本和运维成本。人力成本方面，采用敏捷开发模式，预计 3-4 个月可完成系统开发和测试。硬件成本方面，普通云服务器（2 核 4G）即可满足系统运行需求，年费用约 2000-3000 元。运维成本主要是数据库备份、日志存储等产生的少量存储费用。相比商业智慧农业系统动辄数十万元的投入，本系统成本极低。系统投入使用后，通过精准管理可提高产量 10%-20%，节约人工成本 30% 以上，经济效益显著。预计投资回收期在 6 个月以内。因此，从经济角度看，本项目具有很高的性价比 [53]。

**操作可行性**：

系统基于 Web 浏览器访问，用户无需安装任何客户端软件。界面设计遵循直观易用原则，采用中文菜单和图标，操作流程简洁明了。系统提供详细的使用说明和在线帮助，新用户经过简短培训即可上手使用。移动端适配使得用户可以通过手机随时查看农场数据和接收告警通知。权限管理机制确保不同角色用户只能访问授权功能，保障系统安全。因此，从操作角度看，本系统易于被广大农户接受和使用 [54]。

**社会可行性**：

发展智慧农业符合国家乡村振兴战略和数字中国建设规划，能够得到政策支持和资金补贴。系统推广应用有助于培养新型职业农民，提升农业从业者素质。通过精准管理减少化肥农药使用，有利于保护生态环境，实现农业可持续发展。系统产生的农业数据还可为政府部门制定产业政策提供参考依据。因此，从社会角度看，本项目具有良好的社会效益和推广前景 [55]。

## 3.2 需求分析

### 3.2.1 用户需求分析

通过对农场管理者、农业技术人员、系统管理员等不同用户群体的深入调研，总结出以下核心需求：

**农场管理者需求**：
- 实时掌握农场环境状况，包括温度、湿度、土壤墒情、光照等关键参数
- 及时发现异常情况并收到告警通知，以便快速响应
- 了解环境变化趋势，预判未来几天的情况
- 获得科学的种植建议和管理决策支持
- 查看历史数据，总结生产经验，优化管理方案
- 管理员工账号和权限，确保系统安全
- 操作简单直观，不需要复杂的培训

**农业技术人员需求**：
- 查看详细的历史数据和变化曲线，进行专业分析
- 导出数据用于进一步研究和报告撰写
- 对比不同地块、不同时期的数据差异
- 设置个性化的告警阈值和监测指标
- 利用 AI 工具辅助分析数据，发现潜在问题
- 生成可视化图表，便于汇报展示

**系统管理员需求**：
- 管理系统用户，包括添加、删除、修改用户信息
- 审批用户的管理员权限申请
- 查看系统运行状态，监控服务器资源使用情况
- 查看操作日志，审计用户行为
- 定期备份重要数据，防止数据丢失
- 在系统故障时快速恢复数据和服务
- 配置系统参数和功能模块

**普通农户需求**：
- 用手机就能查看自家农场的数据
- 收到简单易懂的提醒和建议
- 不需要知道复杂的技术细节
- 花费不多，效果明显

综合以上需求，系统需要提供一个功能全面、操作简便、智能高效的农场管理平台，满足不同用户群体的多样化需求。

### 3.2.2 系统功能需求分析

基于用户需求调研，本系统需要实现以下核心功能：

#### 3.2.2.1 用户认证与权限管理

**注册功能**：
- 用户提供用户名、密码、确认密码完成注册
- 密码必须满足复杂度要求：长度至少 8 位，包含大小写字母、数字和特殊字符
- 实时显示密码强度评估（evaluate_password_strength 函数），包括长度检查（≥12 位得 25 分）、字符类型检查（小写字母、大写字母、数字、特殊字符各占权重）、常见模式检测（连续字符、重复字符、键盘模式扣分）
- 注册时需要输入图形验证码，防止机器批量注册。验证码使用 PIL 库生成，包含 4 位随机字符（大小写字母 + 数字），添加干扰线和噪点增强安全性 [9]
- 用户可选择注册为普通用户或申请管理员权限
- 选择管理员需经现有管理员审批通过（admin_request 字段标记，admin_request_time 记录申请时间）

**登录功能**：
- 用户使用用户名和密码登录系统
- 登录时需要输入图形验证码，验证码不区分大小写
- 连续多次登录失败后账户锁定一定时间（基于 login_attempts 字段计数），防止暴力破解
- 登录成功后生成 JWT 令牌，有效期为 14 天（exp 设置为当前时间戳 +12000000 秒）。载荷包含用户名、角色、过期时间三个字段 [43]
- 支持记住登录状态，下次访问自动登录（JWT 存储在 st.session_state 中）
- 登录失败时给出明确提示（用户名不存在、密码错误、验证码错误、账户已被禁用）

**权限管理**：
- 基于角色的访问控制（RBAC），分为普通用户和管理员两种角色
- 不同角色具有不同的功能权限，管理员拥有全部权限（module_config.json 中的 admin_only 字段控制）
- 用户只能访问自己权限范围内的功能模块，未授权访问会重定向到首页
- 支持管理员审批普通用户提交的管理员申请（approve_admin_request 函数）
- 管理员可以修改其他用户的角色和权限（update_user_role 函数）
- 权限变更实时生效，无需重新登录

**个人信息管理**：
- 用户可以修改自己的密码（change_password 函数）
- 修改密码时需要验证原密码，新密码同样需要满足复杂度要求
- 查看个人基本信息和登录历史
- 支持退出登录（清除 session 中的 JWT 令牌）

#### 3.2.2.2 综合监控仪表板

**实时数据显示**：
- 以卡片形式展示当前空气温度、空气湿度、土壤湿度、土壤养分含量、光照强度等关键指标（render_realtime_metrics 函数）
- 每个指标显示当前值、状态（正常/异常）、适宜范围提示
- 数据实时更新，刷新频率可配置（默认每 5 分钟）
- 使用颜色区分正常和异常状态，异常时显示醒目警示色（绿色表示正常，红色表示异常）

**趋势图表展示**：
- 为每个环境参数绘制 24 小时变化趋势曲线（create_trend_chart 函数）
- 曲线图支持缩放、平移、悬停查看具体数值（Plotly 交互功能）
- 在趋势图上用红色 X 标记出异常数据点（基于 IQR 方法检测异常，show_anomalies 参数控制是否显示）[36]
- 叠加显示未来 1-7 天的预测趋势（虚线表示，基于 Prophet 模型，show_predictions 参数控制）
- 显示用户自定义的阈值线（最高值、最低值，水平虚线表示）

**阈值配置**：
- 提供作物生长阶段选择（生长期、开花期、结果期），不同阶段推荐不同的阈值范围
- 根据不同生长阶段自动推荐适宜的阈值范围（如生长期温度 18-28℃，开花期 20-30℃）
- 支持用户手动调整各项参数的告警阈值（custom_thresholds 存储在用户偏好设置中）
- 阈值修改后立即生效并影响告警判断
- 保存用户的个性化阈值配置（保存到 user_preferences 表）

**系统状态监控**：
- 显示服务器 CPU 使用率、内存使用率、磁盘使用率（基于 psutil 库，system_monitoring 模块）[47]
- 显示数据点数量、最后更新时间等系统信息
- 系统异常时自动告警（如 CPU 使用率超过 90%）

**快捷操作**：
- 提供常用功能的快捷入口，如数据分析、模型预测、数据可视化等
- 管理员专属快捷操作：用户管理、数据备份、系统监控、模块配置
- 普通用户快捷操作：数据概览、日志查看等

#### 3.2.2.3 数据概览

**数据来源选择**：
- 支持从数据库读取历史数据（通过 SQLAlchemy 查询 intelligent_farm_airtemperaturehumidity 等表）
- 支持上传本地文件（CSV、Excel、JSON 格式），使用 pandas.read_csv()、read_excel()、read_json() 读取
- 从数据库读取时可选择时间范围（开始日期至结束日期），使用时间戳索引加速查询

**数据预览**：
- 显示数据总行数、列数、缺失值数量（data.shape, data.isnull().sum()）
- 以表格形式展示前若干行数据（st.dataframe 显示，支持滚动）
- 显示每列的数据类型（整数、浮点数、日期时间等，data.dtypes）

**数据筛选**：
- 按时间范围筛选数据（timestamp 字段 BETWEEN 查询）
- 按数据类型筛选（温度、湿度、土壤、光照等，选择特定列）
- 支持多条件组合筛选（AND 逻辑）

**数据导出**：
- 支持导出为 CSV、Excel、JSON 三种格式（to_csv()、to_excel()、to_json()）
- 导出时可选择导出的字段（列选择）
- 导出文件自动命名，包含时间戳信息（如 data_export_20260323_143025.csv）

**数据统计**：
- 显示选中数据的基本统计信息（最大值、最小值、平均值等，describe() 函数）
- 对于异常数据给出提示（基于 3σ原则或箱线图法则识别）

#### 3.2.2.4 数据清洗

**缺失值处理**：
- 自动检测数据中的缺失值
- 提供多种缺失值填充方法：删除、均值填充、中位数填充、前后值填充、插值法
- 显示缺失值处理前后的对比

**异常值处理**：
- 使用箱线图法则或 3σ原则自动识别异常值
- 异常值以红色高亮显示
- 提供异常值处理方式：删除、替换为边界值、替换为中位数
- 记录异常值处理日志

**重复值处理**：
- 检测完全重复或部分重复的数据记录
- 提供重复值删除功能
- 可选择保留第一条或最后一条记录

**数据转换**：
- 支持数据类型转换（字符串转数值、日期格式转换等）
- 支持数据标准化、归一化处理
- 支持创建衍生变量（如计算温差、湿差等）

**数据质量报告**：
- 生成数据质量评估报告
- 显示清洗前后的数据对比
- 给出数据质量改进建议

#### 3.2.2.5 数据分析

**描述性统计**：
- 计算各数值型变量的均值、标准差、最小值、最大值、四分位数
- 以表格形式展示统计结果
- 提供统计指标的通俗解释

**相关性分析**：
- 计算变量间的皮尔逊相关系数矩阵
- 使用热力图可视化相关系数矩阵
- 相关系数用颜色深浅表示，数值标注在热力图中
- 自动识别并提示强相关变量对（相关系数绝对值大于 0.7）

**分组聚合分析**：
- 用户选择分组变量（如日期、时间段等）
- 用户选择聚合变量（如温度、湿度等）
- 用户选择聚合函数（平均值、总和、最大值、最小值）
- 显示分组聚合结果表格
- 根据聚合结果生成柱状图

**数据分布分析**：
- 绘制直方图展示数据分布形态
- 绘制密度曲线拟合数据分布
- 绘制 Q-Q 图检验数据是否服从正态分布
- 计算偏度、峰度等分布特征指标

**智能分析解读**：
- 使用通俗易懂的语言解释统计分析结果
- 指出数据中值得关注的现象和问题
- 提供基于数据的农业管理建议

#### 3.2.2.6 数据可视化

**基础图表**：
- 折线图：展示单个变量随时间的变化趋势
- 柱状图：比较不同类别的数据大小
- 饼图：显示各部分占总体的比例
- 散点图：观察两个变量之间的关系

**高级图表**：
- 热力图：展示多个变量间的相关性
- 箱线图：展示数据分布和异常值
- 小提琴图：结合箱线图和密度曲线的优点
- 雷达图：多维度数据对比

**交互式图表**：
- 所有图表支持鼠标悬停查看详细信息
- 支持图表缩放和平移
- 支持点击图例隐藏/显示某些系列
- 支持下载图表为 PNG 图片

**智能图表推荐**：
- 根据数据类型和分析目的自动推荐合适的图表类型
- 提供多种配色方案供用户选择
- 支持自定义图表标题、坐标轴标签、图例位置

**双 Y 轴图表**：
- 支持在同一图表中显示两个不同量纲的变量
- 左右两侧分别显示不同的 Y 轴刻度
- 适用于分析温度与湿度等相关变量的关系

#### 3.2.2.7 高级分析

**智能分组分析**：
- 用户选择分组列（如按日期分组、按时间段分组）
- 用户选择聚合列（如温度、湿度等）
- 用户选择聚合函数（平均值、总和、最大值、最小值）
- 实时显示分组聚合结果
- 提供结果解读：解释每组数据的含义和差异

**详细图表展示**：
- 根据分组聚合结果自动生成柱状图
- 图表标题和坐标轴标签自动生成
- 支持自定义图表颜色和样式

**智能洞察**：
- 自动找出聚合结果中的最大值和最小值对应的分组
- 分析数据背后的原因和影响
- 提供基于分析结果的 actionable 建议

**多维度分析**：
- 支持两阶段分组（先按 A 列分组，再按 B 列分组）
- 支持交叉表分析
- 支持透视表功能

#### 3.2.2.8 本地数据预测

**预测模型选择**：
- 提供多种预测模型：Prophet+SARIMA混合模型、纯 Prophet 模型、多变量随机森林模型
- 用户根据数据特点选择合适的模型
- 默认推荐使用混合模型以获得更好效果（sarima_validation_prediction 函数）[44][46]

**预测参数配置**：
- 用户设置预测天数（1-30 天）
- Prophet 模型参数调节：变化点灵敏度（changepoint_prior_scale）、季节性强度（seasonality_prior_scale）
- SARIMA 模型参数：AR 阶数 (p)、MA 阶数 (q)、季节性 AR 阶数 (P)、季节性 MA 阶数 (Q)、差分阶数 (d,D)、季节周期 (s)
- 支持手动设置两模型权重或使用自动权重（基于 RMSE 的自适应权重分配算法）

**单变量时间序列预测**：
- 选择要预测的变量（空气温度、空气湿度、土壤湿度等）
- 使用该变量的历史数据训练模型（至少需要 2 个完整周期的数据）
- 生成未来指定天数的预测值
- 绘制历史数据和预测数据的对比图（实线表示历史，虚线表示预测，阴影区域表示置信区间）

**混合预测模型实现**：
- Prophet 基础预测：使用加法模型分解趋势、季节性、残差
- SARIMA 验证与补充：对 Prophet 残差进行 SARIMA 建模，捕捉自相关成分
- 权重动态分配：根据两个模型的 RMSE 计算权重，RMSE 越小权重越大 [19]
  $$w_{prophet} = \frac{1/RMSE_{prophet}}{1/RMSE_{prophet} + 1/RMSE_{SARIMA}}$$
  $$w_{SARIMA} = 1 - w_{prophet}$$
- 结果融合：final_forecast = w_prophet * prophet_forecast + w_SARIMA * sarima_forecast
- 置信区间计算：基于历史预测误差计算 95% 置信区间

**多变量耦合预测**：
- 同时考虑温度、湿度、光照等多个变量的相互作用（multivariate_prediction 函数）
- 使用随机森林等多变量模型（RandomForestRegressor, n_estimators=100）
- 添加滞后特征（lag features）：temp_lag1（前一时段温度）、humid_lag1（前一时段湿度）
- 添加交互项（interaction terms）：temp_humid_interaction = temperature × humidity
- 分析特征重要性（feature_importances_），解释各变量对预测的贡献 [45]

**预测结果展示**：
- 显示预测数据表格（日期、预测值、置信区间上下界）
- 绘制预测曲线图，包含置信区间（Plotly 交互式图表）
- 展示模型评价指标：RMSE（均方根误差）、MAE（平均绝对误差）、R²（决定系数）
- 提供预测精度评级和可信度评分（R²>0.8 为优秀，0.6-0.8 为良好，<0.6 为一般）

**预测结果解读**：
- 分析预测趋势（上升、下降、平稳），基于线性回归斜率
- 评估波动风险（低、中、高），基于标准差
- 给出智能决策建议（强烈推荐、推荐使用、谨慎参考）
- 解释多变量间的耦合关系（如温度升高导致湿度降低）

#### 3.2.2.9 AI 洞察分析

**AI 模型选择**：
- 支持选择不同的 Ollama 模型（qwen3:4b、llama2 等）
- 显示模型可用性状态（check_ollama_available 函数检测）
- 支持一键拉取未安装的模型（pull_model 函数）[48]

**数据背景描述**：
- 用户输入数据的来源、用途等背景信息（如"这是温室番茄生长期的环境数据"）
- AI 结合背景信息进行针对性分析

**AI 数据分析**：
- AI 自动读取数据的基本统计信息（均值、标准差、极值等）
- AI 识别数据中的关键指标和趋势（上升/下降趋势、周期性变化）
- AI 分析变量间的相关关系（正相关、负相关、无相关）
- AI 检测数据中的异常值和模式（离群点、突变点）

**智能解读与建议**：
- AI 用自然语言生成数据分析报告（AIInsightsAnalyzer 类）
- 报告包括 7 个部分 [20]：
  1. **数据概览**：总结数据的基本特征（样本量、时间跨度、变量类型）
  2. **关键指标解读**：分析重要的数值指标及其含义（如平均温度是否适宜作物生长）
  3. **趋势分析**：识别数据中的重要趋势和模式（如温度呈上升趋势）
  4. **关联关系**：解释指标之间的相关性及其农业意义（如温度与湿度负相关）
  5. **异常检测**：指出任何异常值或值得关注的模式（如某天温度突然升高）
  6. **农业建议**：基于数据分析提出具体的操作建议（如加强通风、增加灌溉）
  7. **后续行动**：推荐下一步的数据分析或操作步骤（如继续监测、调整阈值）
- AI 提供个性化建议，回答用户提出的问题

**交互式问答**：
- 用户选择关心的问题方向（如何优化生产、看出什么趋势、如何调整策略等）
- AI 针对具体问题给出详细解答
- 支持多轮对话，深入探讨某个话题

**流式输出**：
- AI 回答采用流式输出（stream_response 函数），逐字显示
- 减少用户等待焦虑
- 提升交互体验
- 支持暂停和继续

#### 3.2.2.10 自动化决策

**规则引擎**：
- 预定义各类环境参数的告警阈值和决策规则（DecisionEngine 类）
- 土壤湿度规则：低于 30% 建议灌溉，高于 60% 无需灌溉
- 温度规则：低于 15℃建议保温，高于 30℃建议通风
- 湿度规则：低于 40% 建议增湿，高于 70% 建议除湿
- 光照规则：低于 1000lux 建议补光 [25]

**趋势分析**：
- 分析过去 24 小时各环境参数的变化趋势（analyze_trends 函数）
- 计算线性回归斜率判断上升或下降趋势（calculate_trend 函数，使用 np.polyfit 拟合）
- 斜率计算公式：slope = Σ((x_i - x̄)(y_i - ȳ)) / Σ((x_i - x̄)²)
- 结合当前值和趋势给出更精准的决策建议

**决策建议生成**：
- 当检测到异常时自动生成决策建议（evaluate_conditions 函数）
- 建议包括：问题描述、原因分析、优先级、推荐措施
- 优先级分为高、中、低三个等级：
  - **高优先级**：当前值低于阈值且呈下降趋势，需立即处理
  - **中优先级**：当前值低于阈值但呈上升趋势，可持续观察
  - **低优先级**：当前值接近阈值但在正常范围内
- 建议附带时间戳和当前值信息（如"当前土壤湿度 25.3%，时间 2026-03-23 14:30"）

**趋势增强决策示例**：
- 若土壤湿度 < 30% 且 slope < 0（下降趋势）：生成"土壤湿度低于 30%(25.3%) 且呈下降趋势，建议立即灌溉"
- 若土壤湿度 < 30% 但 slope > 0（上升趋势）：生成"土壤湿度低于 30%(28.1%) 但呈上升趋势，可能刚完成灌溉，建议继续观察"
- 原因分析：详细说明判断依据，包括当前值、阈值、趋势斜率等

**决策历史记录**：
- 记录每次生成的决策建议（决策内容、优先级、时间戳）
- 用户可查看历史决策和改进措施
- 支持对决策效果进行评估（决策后数据变化对比）

**规则自定义**：
- 允许用户根据实际情况修改决策规则的阈值
- 支持添加自定义决策规则
- 规则修改需经管理员权限

#### 3.2.2.11 用户管理（管理员专用）

**用户列表**：
- 以表格形式展示所有用户信息（ID、用户名、角色、管理员申请状态）
- 支持按用户名、角色筛选用户
- 显示用户注册时间、最后登录时间

**添加用户**：
- 管理员可直接添加新用户
- 设置用户名、初始密码、角色
- 新密码需满足复杂度要求

**编辑用户**：
- 修改用户名和角色
- 重置用户密码
- 启用或禁用用户账号

**删除用户**：
- 删除不再需要的用户账号
- 删除前进行二次确认
- 记录删除操作日志

**管理员申请审批**：
- 查看待审批的管理员申请列表
- 批准或拒绝用户的申请
- 审批通过后用户获得管理员权限
- 审批拒绝后用户保持普通用户身份

#### 3.2.2.12 系统监控（管理员专用）

**实时资源监控**：
- CPU 使用率：实时显示 CPU 占用百分比
- 内存使用率：显示已用内存和总内存
- 磁盘使用率：显示各磁盘分区使用情况
- 网络流量：显示网络收发速率

**进程监控**：
- 显示 Streamlit 应用的进程 ID
- 显示进程占用的 CPU 和内存资源
- 列出相关 Python 进程

**性能分析**：
- 分析页面加载时间
- 分析数据库查询耗时
- 识别性能瓶颈

**告警通知**：
- CPU 或内存使用率超过阈值时发出告警
- 磁盘空间不足时发出告警
- 应用异常崩溃时发出告警

#### 3.2.2.13 日志查看

**日志列表**：
- 以表格形式展示系统操作日志
- 显示日志时间、级别、用户名、操作类型、操作详情
- 支持按时间范围、日志级别、用户名、操作类型筛选

**日志详情**：
- 点击查看单条日志的详细信息
- 显示操作的完整上下文信息

**日志统计**：
- 统计各级别日志数量（INFO、WARNING、ERROR）
- 统计各用户操作频次
- 统计各功能模块使用频率

**日志导出**：
- 支持将日志导出为文本文件
- 导出时可选择时间范围和日志级别

**日志分析**：
- 分析日志中的错误模式和趋势
- 识别高频操作和异常行为
- 生成日志分析报告

#### 3.2.2.14 数据备份（管理员专用）

**备份配置**：
- 选择备份的数据表（空气温湿度、土壤湿度、土壤养分、光照强度等）
- 设置备份时间范围（开始时间至结束时间）
- 选择备份文件格式（SQL 脚本、CSV 文件等）

**备份执行**：
- 点击备份按钮启动备份任务
- 显示备份进度条
- 备份完成后显示备份文件大小和保存位置

**备份历史**：
- 查看历史备份记录
- 显示备份时间、备份内容、文件大小等信息
- 支持删除过期的备份文件

**定时备份**：
- 配置定时备份任务（每天、每周、每月）
- 设置备份保留策略（保留最近 N 份备份）
- 系统自动执行定时备份任务

#### 3.2.2.15 数据恢复（管理员专用）

**备份文件选择**：
- 从备份列表中选择要恢复的备份文件
- 显示备份文件的详细信息（时间、内容、大小）

**恢复预览**：
- 预览备份文件中的数据
- 确认恢复的数据范围

**恢复执行**：
- 选择恢复模式（覆盖恢复或追加恢复）
- 点击恢复按钮开始恢复数据
- 显示恢复进度
- 恢复完成后显示恢复结果统计

**恢复验证**：
- 恢复后自动验证数据完整性
- 检查恢复的数据是否可以正常访问
- 生成恢复报告

#### 3.2.2.16 模块配置管理（管理员专用）

**模块列表**：
- 以卡片形式展示所有功能模块（ModuleManager 类管理）
- 显示模块名称、图标、描述、分类、启用状态
- 标识管理员专用模块（admin_only=true 的模块）
- 模块信息来源于 module_config.json 配置文件 [26][89]

**模块启用/禁用**：
- 通过开关按钮启用或禁用模块（toggle_switch 组件）
- 模块状态改变立即生效，无需重启系统
- 禁用的模块在侧边栏菜单中不显示（check_module_enabled 函数检测）
- 启用模块时自动检查依赖关系

**依赖管理**：
- 显示模块间的依赖关系（dependencies 字段定义）
- 启用模块时自动检查依赖是否满足，若依赖模块未启用则提示用户
- 禁用模块时提示受影响的依赖模块（如禁用 data_overview 会影响 data_analysis）
- 依赖检查逻辑：遍历 dependencies 列表，检查每个依赖模块的 enabled 状态

**模块分类**：
- 按功能分类展示模块（8 大分类）[89]：
  - **核心功能**：integrated_dashboard（综合监控仪表板）
  - **数据处理**：data_overview（数据概览）、data_cleaning（数据清洗）、data_preview（数据预览）
  - **数据分析**：data_analysis（统计分析）、enhanced_analysis（高级分析）、visualization（数据可视化）
  - **预测分析**：data_prediction（本地数据预测）
  - **智能分析**：ai_insights（AI 洞察分析）、decision_engine（自动化决策）
  - **系统管理**：user_management（用户管理）、system_monitoring（系统监控）、log_viewer（日志查看）、module_config（模块配置）
  - **数据管理**：backup（数据备份）、restore（数据恢复）
  - **帮助**：instruction_manual（使用说明）
- 支持按分类筛选模块（下拉菜单选择）

**模块搜索**：
- 通过关键词搜索模块（search 输入框）
- 快速定位目标模块（实时过滤，匹配 name 和 display_name 字段）

**配置导入导出**：
- 导出当前模块配置为 JSON 文件（export_config 函数）
- 从 JSON 文件导入模块配置（import_config 函数）
- 支持配置重置为默认值（reset_to_defaults 函数）
- 配置文件包含所有模块的 enabled 状态

### 3.2.3 系统非功能需求分析

**性能需求**：
- 页面加载时间：首屏加载时间不超过 3 秒
- 数据查询响应时间：简单查询不超过 1 秒，复杂查询不超过 5 秒
- 并发用户数：支持至少 50 个用户同时在线使用
- 数据更新频率：实时数据每 5 分钟更新一次
- 预测模型训练时间：单次训练不超过 30 秒
- 系统可用性：系统可用率达到 99% 以上

**安全性需求**：
- 用户密码加密存储，使用 bcrypt 算法
- 通信加密，使用 HTTPS 协议
- 防止 SQL 注入攻击，使用参数化查询
- 防止 XSS 跨站脚本攻击，对用户输入进行转义
- 防止 CSRF 跨站请求伪造，使用 CSRF Token
- 会话超时自动登出，超时时间设置为 2 小时
- 敏感操作需要二次确认（如删除用户、恢复数据）
- 操作日志完整记录，可追溯审计

**可靠性需求**：
- 系统平均无故障时间（MTBF）大于 1000 小时
- 关键功能（如数据采集、告警通知）不出现失效
- 数据库定期自动备份，数据不丢失
- 系统具有容错能力，单个模块故障不影响整体运行
- 异常情况有明确的错误提示和处理机制

**易用性需求**：
- 界面简洁美观，符合现代 UI 设计规范
- 操作流程符合用户习惯，减少学习成本
- 提供清晰的引导和提示信息
- 支持快捷键操作，提高熟练用户效率
- 响应式设计，适配不同分辨率的屏幕
- 提供在线帮助和使用教程

**可维护性需求**：
- 代码结构清晰，注释完整，符合编码规范
- 采用模块化设计，模块间接口明确
- 配置文件与代码分离，便于部署和维护
- 提供完善的日志记录功能，便于问题排查
- 支持热更新，修复 bug 后无需重启整个系统

**可扩展性需求**：
- 系统架构支持水平扩展，可通过增加服务器提升性能
- 模块化设计便于新增功能，不影响现有功能
- 数据库设计预留扩展字段，适应业务变化
- 提供 API 接口，便于与第三方系统集成
- 支持插件机制，用户可自行开发扩展模块

**兼容性需求**：
- 支持主流浏览器：Chrome、Firefox、Safari、Edge
- 支持移动端访问，iOS 和 Android 系统
- 支持 MySQL 5.7 及以上版本
- 兼容 Python 3.8 及以上版本
- 支持 Windows、Linux、macOS等操作系统

---